kubecost:
  # Network-costs runs a DaemonSet on every node; on the small default nodes it
  # does not fit and leaves pods pending. Disabled for this MVP cluster.
  networkCosts:
    enabled: true
  serviceAccount:
    create: true
    name: kubecost-aws
  cloudCost:
    serviceAccountName: kubecost-aws
